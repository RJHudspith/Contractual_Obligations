/**
   @file progress_bar.h
   @brief prototype declarations for the progress bar
 */
#ifndef PROGRESS_BAR_H
#define PROGRESS_BAR_H

/**
   @fn void progress_bar( const size_t state , const size_t goal )
   @brief print to stdout the progress bar
 */
void
progress_bar( const size_t state ,
	      const size_t goal ) ;

#endif
